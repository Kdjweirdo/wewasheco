json.extract! renseignement, :id, :entreprise, :string, :particulier, :string, :email, :string, :numero, :string, :description, :text, :created_at, :updated_at
json.url renseignement_url(renseignement, format: :json)
